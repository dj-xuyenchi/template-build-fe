export interface BtnDTO {
  btnId: string;
  btnCode: string;
  btnName: string;
  btnDescription: string;
  maker: string;
  updatedBy: string;
  createdAt: string;
  updatedAt: string;
  status: string;
}
